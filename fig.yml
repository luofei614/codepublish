phpmyadmin:
  build: ./runbox
  volumes:
   - ./phpmyadmin:/app
  links:
   - db
   - memcache
  environment:
   - VIRTUAL_HOST=phpmyadmin.i.shequnnet.com
luofeitest:
  build: ./runbox
  volumes:
   - /code/luofei.test:/app
  links:
   - db
   - memcache
  environment:
   - VIRTUAL_HOST=luofei.test.i.shequnnet.com
db:
  image: tutum/mariadb
  environment:
   - MARIADB_PASS=sansheng20150214 
memcache: 
  image: memcached
